<!-- 管理者側商品一覧 -->

<div style="text-align: center; margin-bottom: 30px;">
<h1>商品一覧</h1>
</div>
<div class="container">
	<% if flash[:notice] %>
		  <p><%= flash[:notice] %></p>
			<% end %>
	<div class="row">
		<div class="col-lg-12">
			<% @items.each do |item| %>
			<div id="box">
				<table class="item-box">
					<tbody>
					<tr>
					  <td>
						<%= attachment_image_tag item, :item_image, :fill, 200, 150, format: 'jpeg', fallback: "no_image.jpg" %>
					  </td>
					</tr>
					<tr>
					  <td><%= link_to item.item_name, admin_item_path(item[:id]) %></td>
					</tr>
					<tr>
					  <td><%= item.price %></td>
					</tr>
					<tr>
					  <td><%= item.item_status %></td>
					</tr>
					</tbody>
				</table>
			</div>
			<% end %>
		</div>
	</div>
</div>